{% extends 'base.html.twig' %}
{% block title %}Catégorie {{ categoryName }}{% endblock %}

{% block body %}
    <div class="container-fluid">
        <h1>Séries de la catégorie : {{ categoryName }} ({{ category.id }})</h1>

    {% for program in programs %}
    <div>
        <h2>{{ loop.index }} / {{ program.title }} -  Catégorie : {{ program.category }}</h2>
        <p>{{ program.summary }}</p>
        <img class="align-self-sm-start" src="{{ program.poster }}" alt="{{ program.title }} poster">
    </div>
    {% else %}
        Aucune série trouvée.
    {% endfor %}
    <a href="{{ path('app_index') }}">
        Retour à l'acceuil
    </a>

{% endblock %}